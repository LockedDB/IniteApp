import { StyleSheet } from 'react-native';
import { Black } from '../../../../Utils/colors';

export default StyleSheet.create({
  topBarContainer: {
    padding: 16,
    paddingBottom: 8,
  },
  title: {
    fontSize: 24,
    lineHeight: 28,
  },
});
