import { EerieBlack } from '@utils/colors';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    borderRadius: 100,
    height: 40,
    width: 40,
    borderWidth: 2,
    borderColor: EerieBlack,
  },
});
